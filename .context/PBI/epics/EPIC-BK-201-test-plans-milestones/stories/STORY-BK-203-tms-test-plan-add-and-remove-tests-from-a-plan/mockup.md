# BK-203 — Mockup

> Jira field: `customfield_10137` · [View in Jira](https://jira.upexgalaxy.com/browse/BK-203)

***Design intent — mockup pending***

- Screen: "Test Plan detail — membership" (extends the plan detail tab).
- Layout: header block on top; below it the member-tests table fills the tab; "Add tests" button on the header's right edge.
- Primary components: add-tests dialog (search field, result list with checkboxes and already-included markers, confirm bar with selected count), member table, per-row kebab with Remove, confirm popover.
- Key interactions: search-select-confirm loop, per-row removal, count updating live in header and list.

---
_Synced from Jira by sync-jira-issues_

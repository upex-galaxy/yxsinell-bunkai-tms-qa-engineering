# BK-228 — Workflow

> Jira field: `customfield_10082` · [View in Jira](https://jira.upexgalaxy.com/browse/BK-228)

Sara pushes a change to her feature branch and opens the PR. The pipeline runs the regression Tests and reports the runs to Bunkai with the commit, branch, and pipeline attached. One Test fails. From the PR, she opens Bunkai, filters runs by her branch, and sees the failing run pinned to her exact commit — yesterday's run on the previous commit was green. She opens the commit link, spots the culprit, fixes it, and pastes the Bunkai run link into the PR discussion so the reviewer sees the before/after evidence.

---
_Synced from Jira by sync-jira-issues_

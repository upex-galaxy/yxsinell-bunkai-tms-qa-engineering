# Comments for BK-3

[View in Jira](https://jira.upexgalaxy.com/browse/BK-3)

---

### Ely - 20/5/2026, 2:05:41

🧱 ****Architect Annotation****

**Posted by repo automation. Sections below are the architecture-grade complement to the user-facing fields (description / AC / Scope / Business Rules / Workflow). Source-of-truth on dev-side concerns — synced to local `comments.md` by `sync-jira-issues`.**

1. 

- Buttons on `/login` page: `<OAuthButton provider="github" />`, `<OAuthButton provider="google" />`.
- Callback: `app/auth/callback/route.ts` (shared with magic-link callback).

1. 

- `supabase.auth.signInWithOAuth({ provider, options: { redirectTo } })`.
- Validate `state` query param.

1. 

- GitHub OAuth app (client*id, client*secret in env).
- Google OAuth client (client*id, client*secret in env).

1. 

- Supabase OAuth providers configured (GitHub + Google apps registered in Supabase dashboard).

1. 

- [https://jira.upexgalaxy.com/browse/BK-2#icft=BK-2](https://jira.upexgalaxy.com/browse/BK-2#icft=BK-2) (email magic-link) — same FR-001.

1. 

- [ ] Both providers (GitHub + Google) tested in staging.
- [ ] CSRF state-mismatch path tested.
- [ ] Magic-link fallback surfaced when OAuth blocked.
- [ ] E2E Playwright test for at least one provider happy-path.
- [ ] Documentation: README "Getting Started" mentions OAuth.

---

### Luis Eduardo Flores Villarroel - 26/5/2026, 2:49:18

## Shift-Left Refinement Mirror — [https://jira.upexgalaxy.com/browse/BK-3#icft=BK-3](https://jira.upexgalaxy.com/browse/BK-3#icft=BK-3) OAuth

***Shift-Left batch session****: 2026-05-26 | ****Label***: `shift-left-reviewed`

Full artifact: `.context/PBI/tenancy-identity/BK-3-oauth-signup/shift-left-refinement.md` in `upex-bunkai-tms` repo.

---

### Story Quality Verdict: Needs Improvement

- 7 AC gaps (G1–G7)
- 5 ambiguities (A1–A5)
- 2 contradictions (C1: `/home` vs `/projects` redirect; C2: OAuth buttons labeled "soon")
- 15 edge cases surfaced
- 20 test outlines drafted (see ATP field)

---

### Critical: OAuth Buttons Are Disabled in Current UI

`app/(auth)/login/page.tsx` — GitHub and Google buttons are `disabled` with label "soon". ***BK-3 cannot be tested until these are enabled.*** Enabling the buttons must be in-scope for this story.

---

### Contradictions (block sprint planning)

|  | Contradiction  |
| --- |
| --- | --- |
| C1  | Story says redirect to `/home`; callback code defaults to `/projects`. One is wrong — reconcile before Dev starts.  |
| C2  | Login page says "OAuth ships next sprint" — if [https://jira.upexgalaxy.com/browse/BK-3#icft=BK-3](https://jira.upexgalaxy.com/browse/BK-3#icft=BK-3) is the OAuth ticket, UI copy + button enable are in-scope.  |

---

### Critical Questions for PO

1. ***Canonical redirect after OAuth success***: `/home` or `/projects`?
2. `EMAIL_EXISTS` ***error code and user-visible message*** — exact string + redirect URL?
3. ***Invited-user first login***: skip personal workspace bootstrap if invite row exists for that email?

---

### New ACs Added (7 refined scenarios)

Key additions to original story ACs:

- `Scenario 2.1` — State token tampered → redirect `/login?error=otp*exchange*failed` (no session)
- `Scenario 2.2` — Missing state token → same
- `Scenario 3.1` — `EMAIL*EXISTS` collision → `/login?error=email*exists` (exact code TBD)
- `Scenario 4.1` — User denies consent → graceful error + magic-link CTA
- `Scenario 4.2` — Provider 5xx → same
- `Scenario 1.3` — Returning user: no duplicate workspace
- `Scenario 5.1` — Redirect to `/home` (pending C1 resolution)

---

### Test Data Requirements (provision before sprint)

- GitHub OAuth test app + Google OAuth credentials in Supabase staging (redirect URI `{STAGING_URL}/auth/callback`)
- Shared-email test account: one email verified on BOTH GitHub AND Google (for `EMAIL_EXISTS` TC)
- Supabase staging project with OAuth providers enabled

---

**Generated by Shift-Left QA pre-sprint batch. When story reaches Ready For QA,** `/sprint-testing` **will detect** `shift-left-reviewed` **and skip redundant refinement phases.**

---

### Andrés Daniel Cumare Morales - 27/5/2026, 1:56:57

## === Shift-Left Refinement Mirror: [https://jira.upexgalaxy.com/browse/BK-3#icft=BK-3](https://jira.upexgalaxy.com/browse/BK-3#icft=BK-3) ===

***Refined on****: 2026-05-26 | ****QA mode***: Shift-Left pre-sprint batch

***Verdict***: Needs Improvement — 10 AC gaps, 5 ambiguities, 2 contradictions, 3 codebase implementation gaps found

---

## Summary

[https://jira.upexgalaxy.com/browse/BK-3#icft=BK-3](https://jira.upexgalaxy.com/browse/BK-3#icft=BK-3) is a ***complete greenfield OAuth implementation****. Zero OAuth code exists in the codebase today. All three layers (UI, client, server) must be built from scratch. The original 5 ACs are insufficient for sprint commitment. ****5 blocker questions for PO/Dev must be resolved before sprint planning.***

---

## Critical Findings

### Codebase Implementation Gaps (NEW)

| ***#**** | ****Gap**** | ****File*** |
| --- | --- | --- |
| G8 | `app/auth/callback/route.ts` handles magic-link OTP only — no OAuth state validation, redirects to `/projects` not `/home` | `app/auth/callback/route.ts` |
| G9 | No `signInWithOAuth` call anywhere; `auth-context.tsx` exposes only `signInWithMagicLink` + `signOut` | `components/providers/auth-context.tsx` |
| G10 | OAuth buttons `disabled={true`}, no `onClick` handler, copy says "OAuth ships next sprint" | `app/(auth)/login/page.tsx` lines 136–155 |

### Contradictions

| ***#**** | ****Contradiction*** |
| --- | --- |
| C1 | Story workflow says redirect to `/home`; callback route defaults to `/projects` — must reconcile before Dev implements |
| C2 | Login page copy says "OAuth ships next sprint" and buttons are disabled — UI copy + button enable are in-scope for [https://jira.upexgalaxy.com/browse/BK-3#icft=BK-3](https://jira.upexgalaxy.com/browse/BK-3#icft=BK-3) |

---

## Open Questions for PO / Dev (BLOCK sprint planning)

***Q1**** — Canonical post-OAuth redirect: `/home` or `/projects`? **(C1)*

***Q2**** — Observable evidence of workspace creation (URL, UI element, DB-only)? **(A5)*

***Q3**** — `EMAIL_EXISTS` user-facing behavior: URL param, toast, error page? HTTP status? **(G2)*

***Q4**** — OAuth callback route strategy: extend existing `app/auth/callback/route.ts` or dedicated `app/auth/oauth/callback/route.ts`? **(G8)*

***Q5**** — Workspace bootstrap failure: rollback session, onboarding screen, or lazy creation? **(G3)*

---

## Refined ACs: 10 scenarios (5 original refined + 5 new)

| ***AC**** | ****Scenario**** | ****Status*** |
| --- | --- | --- |
| AC-1 | GitHub OAuth first-time sign-up (happy path) | Refined |
| AC-2 | Google OAuth first-time sign-up (happy path) | Refined |
| AC-3 | Returning OAuth user sign-in — no duplicate | ***NEW*** [NEEDS PO/DEV CONFIRMATION] |
| AC-4 | OAuth consent denied | Refined |
| AC-5 | OAuth state CSRF token mismatch → 403 | Refined |
| AC-6 | Third-party cookie restrictions → 30s fallback | Refined |
| AC-7 | EMAIL_EXISTS — cross-provider collision | ***NEW*** [NEEDS PO/DEV CONFIRMATION] |
| AC-8 | Workspace bootstrap failure | ***NEW*** [NEEDS PO/DEV CONFIRMATION] |
| AC-9 | OAuth initiation failure | ***NEW*** [NEEDS PO/DEV CONFIRMATION] |
| AC-10 | UI buttons enabled + copy updated | ***NEW*** [NEEDS PO/DEV CONFIRMATION] |

---

## ATP DRAFT — 20 Outlines

***Positive (5)***: GitHub first-time sign-up · Google first-time sign-up · GitHub returning user · Google returning user · OAuth buttons enabled + copy updated

***Negative (7)***: Consent denied GitHub · Consent denied Google · CSRF mismatch 403 · EMAIL*EXISTS via Google · EMAIL*EXISTS via GitHub · OAuth initiation failure · Workspace bootstrap failure

***Boundary (3)***: Cookie restriction 30s cutoff · Simultaneous OAuth+magic-link · Expired code reuse

***Integration (5)***: GitHub full Supabase flow · Google full Supabase flow · First login workspace DB rows · Session cookie attributes · Rate-limit/lockout on CSRF probing

---

**Full refinement file: `.context/PBI/auth/BK-3-oauth-github-google/shift-left-refinement.md`**

**Labels: `shift-left-reviewed` `shift-left-2026-05-26`**

---

### Andrés Daniel Cumare Morales - 27/5/2026, 2:28:54

## 🎯 PO Response — [https://jira.upexgalaxy.com/browse/BK-3#icft=BK-3](https://jira.upexgalaxy.com/browse/BK-3#icft=BK-3) Blocker Questions

***Role***: Product Owner (simulated for QA engineering practice)

***Date***: 2026-05-27

> This comment answers Q1, Q2, Q3, Q5 raised in the Shift-Left refinement. Responding as the Product Owner who owns the business intent, user journey, and acceptance of this story.

---

### Q1 — Canonical post-OAuth redirect: `/home` or `/projects`?

***CONFIRMED: The story is wrong. Correct redirect is `/onboarding` for first-time users, `/projects` for returning users.***

Reasoning from the product model:

- `/home` does not exist as a route. `app/page.tsx` redirects root `/` to `/login`. There is no "home" page — the app's entry point after authentication is either the onboarding flow or the projects list.
- The IQL model is workspace-gated. A user without a workspace cannot use the product. First-time OAuth users have no workspace yet, so they must land on `/onboarding` to create one — identical to the magic-link first-login flow.
- Returning OAuth users already have a workspace → `/projects` directly.

***Updated post-OAuth redirect decision:***

| ***User type**** | ****First OAuth login**** | ****Returning OAuth login*** |
| --- | --- | --- |
| Has workspace | — | `/projects` |
| No workspace | `/onboarding` | — |

AC-1 and AC-2 must be updated to reflect this. The `/home` reference in the workflow is a copy error from an earlier spec draft — discard it.

---

### Q2 — Observable evidence of workspace creation

***CONFIRMED: Two observable signals, in priority order:***

1. ***Primary (UI)***: User lands on `/onboarding`, fills the workspace form, submits, and is then redirected to `/projects` — where the workspace name appears in the app sidebar/header. This is the testable end state: user is on `/projects` with a visible workspace.
2. ***Secondary (URL)***: The URL path `/projects` after onboarding completion is sufficient as a test assertion — does not require DB inspection.

For the integration outline (outline 18), a DB assertion via `workspace_members` table is acceptable to verify the row was created. QA can use the staging DB MCP for that specific outline.

***Do NOT*** require direct `auth.users` inspection as an assertion — that table is Supabase-managed and not directly queryable from the app layer.

---

### Q3 — EMAIL_EXISTS user-facing behavior

***CONFIRMED: URL error param + Sonner toast. No page redirect to an error page.***

Pattern must be consistent with existing error handling in `app/auth/callback/route.ts`:

- Browser redirect: `/login?error=email_exists`
- UI: Sonner toast (already in the stack, `sonner 2.0`) renders from the `error` query param on the login page
- Message: **"An account with this email already exists via a different provider. Contact support to link accounts."**
- No raw HTTP 4xx to the browser — the browser always receives a redirect (3xx); the error is in the URL param

HTTP status from the server to the browser: ***302 redirect*** to `/login?error=email_exists`. The error handling is client-side (toast on mount if `error` param present).

---

### Q5 — Workspace bootstrap failure behavior

***CONFIRMED: Option (b) — persist session, redirect to `/onboarding`. Do NOT rollback.***

Rationale:

- OAuth code exchange is an irreversible Supabase Auth operation. Rolling back the session after a successful code exchange would mean the user is authenticated with the provider but not in our system — creating a worse ghost-user scenario than the bootstrap failure itself.
- The onboarding page already handles the "authenticated but no workspace" state — it shows the workspace creation form. If bootstrap fails server-side, redirect the authenticated user to `/onboarding` and let them complete workspace creation there.
- Log the failure server-side with the user ID and error details for ops visibility.

***Decision***: POST OAuth success → attempt workspace auto-creation → if fails → redirect to `/onboarding` (do not redirect to `/login`, do not rollback session).

---

**As PO I confirm these decisions are final and unblock Dev estimation. Story workflow section must be updated before sprint start.**

---

### Andrés Daniel Cumare Morales - 27/5/2026, 2:29:27

## ⚙️ Dev Response — [https://jira.upexgalaxy.com/browse/BK-3#icft=BK-3](https://jira.upexgalaxy.com/browse/BK-3#icft=BK-3) Blocker Questions

***Role***: Lead Developer (simulated for QA engineering practice)

***Date***: 2026-05-27

***Stack context***: Next.js 15 App Router · Supabase Auth (`@supabase/ssr 0.10.3`) · TypeScript 5.9.3 · Vercel

> Answering Q1 and Q4 as the developer who will implement this story. Confirming PO decisions on Q3 and Q5 from an implementation feasibility standpoint.

---

### Q1 — Confirmed (Dev implementation perspective)

PO decision confirmed. `/home` does not exist — `app/page.tsx` is a one-liner that does `redirect('/login')`. Implementing it would be scope creep.

***Implementation decision for the callback route:***

The correct logic mirrors `app/(app)/onboarding/page.tsx` exactly:

```
// After exchangeCodeForSession succeeds:
const { data: memberships } = await supabase
  .from('workspace_members')
  .select('workspace_id')
  .eq('user_id', user.id)
  .eq('status', 'active')
  .limit(1);

const redirectTarget = (memberships && memberships.length > 0)
  ? '/projects'
  : '/onboarding';

return NextResponse.redirect(`${origin}${redirectTarget}`);
```

This is the pattern already established in the codebase. No new logic to invent — just port it to the callback route.

---

### Q4 — OAuth callback route strategy: extend or dedicated?

***CONFIRMED: Extend the existing `app/auth/callback/route.ts`. Single route, branch on `state` param.***

Technical rationale:

1. ***Supabase PKCE handles both flows with the same method.*** `supabase.auth.exchangeCodeForSession(code)` works for both magic-link OTP and OAuth PKCE — Supabase's SDK handles the distinction internally. We do not need to call a different method.

1. ***Single Supabase redirect URI is simpler.*** Having one redirect URI (`${NEXT*PUBLIC*APP_URL}/auth/callback`) registered in both the GitHub OAuth app and Google OAuth app — same as what magic-link already uses — reduces configuration surface. A dedicated `/auth/oauth/callback` route would require registering two additional URIs in Supabase dashboard AND in both provider OAuth apps. More config = more misconfiguration risk.

1. ***State parameter is the discriminator.*** OAuth callbacks always carry `?state=...`. Magic-link callbacks never do. The branch is clean:

```
export async function GET(request: NextRequest) {
  const { searchParams, origin } = new URL(request.url);
  const code = searchParams.get('code');
  const state = searchParams.get('state'); // present = OAuth PKCE, absent = magic-link OTP
  const next = searchParams.get('next') ?? '/projects';

  // Open-redirect guard (unchanged)
  const safeNext = next.startsWith('/') && !next.startsWith('//') ? next : '/projects';

  if (!code) {
    return NextResponse.redirect(`${origin}/login?error=missing_code`);
  }

  // State validation for OAuth (skip for magic-link — no state param)
  if (state) {
    const storedState = // retrieve from httpOnly cookie set at OAuth initiation
    if (state !== storedState) {
      return NextResponse.redirect(`${origin}/login?error=oauth*state*mismatch`);
    }
  }

  const supabase = await createClient();
  const { error } = await supabase.auth.exchangeCodeForSession(code);

  if (error) {
    // Map known Supabase error messages to our error codes
    if (error.message?.includes('email already')) {
      return NextResponse.redirect(`${origin}/login?error=email_exists`);
    }
    const reason = encodeURIComponent(error.message);
    return NextResponse.redirect(`${origin}/login?error=otp*exchange*failed&reason=${reason}`);
  }

  // Post-auth redirect: check workspace membership
  const { data: { user } } = await supabase.auth.getUser();
  if (user) {
    const { data: memberships } = await supabase
      .from('workspace*members').select('workspace*id')
      .eq('user_id', user.id).eq('status', 'active').limit(1);
    if (!memberships || memberships.length === 0) {
      return NextResponse.redirect(`${origin}/onboarding`);
    }
  }

  return NextResponse.redirect(`${origin}${safeNext}`);
}
```

1. ***State token storage***: use an httpOnly cookie set at OAuth initiation time (in the `signInWithOAuth` client call), validated and cleared on callback. Supabase's PKCE helper (`supabase.auth.signInWithOAuth` with `flowType: 'pkce'`) handles state generation natively — we should use that rather than rolling our own.

---

### Q3 — EMAIL_EXISTS (Dev feasibility confirmation)

PO decision (URL param + Sonner toast) is implementable. One note: Supabase Auth may not surface a clean `email*exists` error code — it may return a generic error message. The callback route must inspect the error message string and map it to our canonical `email*exists` code. This mapping must be tested — it is brittle if Supabase changes their error messages across SDK versions.

***Recommendation***: add a test for the error mapping in the integration test suite (outline 9/10), not just the UI assertion.

---

### Q5 — Workspace bootstrap failure (Dev feasibility confirmation)

PO decision (option b — redirect to `/onboarding`) confirmed as the correct implementation. The onboarding page already performs the `workspace_members` check on mount, so a failed auto-creation during OAuth will naturally cause the user to see the onboarding form when they reach `/onboarding`. No extra state management needed.

Server-side logging: use `console.error` tagged with `[oauth:workspace-bootstrap]` + `userId` + error details. If structured logging is added to the project later, this tag makes it filterable.

---

**As Lead Dev I confirm Q4 is unblocked and implementation can proceed once PO decisions on Q1/Q3/Q5 are accepted in the sprint planning session. Estimated additional scope from codebase gaps G8–G10: +2 story points above original estimate.**

---

### Andrés Daniel Cumare Morales - 27/5/2026, 2:30:06

## 🎨 Design Response — [https://jira.upexgalaxy.com/browse/BK-3#icft=BK-3](https://jira.upexgalaxy.com/browse/BK-3#icft=BK-3) Blocker Questions

***Role***: Product Designer (simulated for QA engineering practice)

***Date***: 2026-05-27

***Design context***: DESIGN.md — dark-first, information density, Sonner toasts, `--fail` signal token, Inter + JetBrains Mono, `--accent` vermillion `#d9543f`

> Answering the design-facing aspects of Q2, Q3, and Q5. Providing component-level decisions so QA can write precise UI assertions.

---

### Q2 — Visible workspace creation signal (Design specification)

***CONFIRMED: Workspace name visible in the app sidebar after first-login OAuth flow.***

The onboarding form (`onboarding-form.tsx`) collects a workspace slug. After submission, the user lands on `/projects` — the app shell at that point renders the workspace name in the top-left of the sidebar (workspace switcher area). This is the primary visual confirmation.

***QA assertion target:***

- Element: workspace display name in the sidebar header
- Condition: visible, non-empty, matches the name the user entered during onboarding
- If auto-created (slug generated from OAuth display name): assert the slug is present and non-empty in the sidebar

***Do not*** assert workspace creation via URL alone — URL is a secondary signal. The UI element is the canonical observable.

---

### Q3 — EMAIL_EXISTS UI treatment (Design specification)

***CONFIRMED: Sonner toast, destructive variant. NOT an inline form error, NOT a full error page.***

Design rationale:

- The login screen uses `magic-link-form.tsx` as its primary interactive surface. The OAuth buttons sit below a divider (`OR`), outside the form. An inline form error would be misleading because no form field caused the error.
- A full redirect to an error page breaks the flow and is disproportionate for a recoverable scenario (user can try magic-link instead).
- Sonner (already in stack at `sonner 2.0`) is the correct component — it renders above all content without disrupting layout.

***Exact toast specification:***

| ***Attribute**** | ****Value*** |
| --- | --- |
| Variant | `toast.error(...)` (destructive — maps to `--fail #e5484d` background tint) |
| Title | `"Account already exists"` |
| Description | `"This email is registered via a different provider. Contact support to link accounts."` |
| Duration | `6000ms` (error toasts stay longer than info toasts) |
| Position | `top-center` (consistent with other auth-flow feedback in the login screen) |
| Dismiss | Manual X + auto-dismiss at 6s |

***Additionally***: the OAuth button that was clicked should briefly show an error state — `data-variant="danger"` (outlined red, per DESIGN.md §6 Button vocabulary) for 2–3s, then revert to disabled. This gives spatial context ("that button caused the error") even as the toast is read.

***QA assertion for outline 9/10:***

```
1. Click "Continue with Google" (or GitHub) with a cross-registered email
2. Wait for redirect back to /login?error=email_exists
3. Assert: Sonner toast is visible with title "Account already exists"
4. Assert: toast is in error/destructive styling (red tint)
5. Assert: toast disappears after 6s OR user can dismiss manually
6. Assert: login screen is still interactive (magic-link form still usable)
```

---

### Q5 — Workspace bootstrap failure UI (Design specification)

***CONFIRMED: Inline error state within the onboarding flow. NOT a full error page. NOT a toast.***

Design rationale:

- The onboarding screen (`onboarding/page.tsx`) is a focused, single-purpose surface. If workspace creation fails at the RPC level, the error is scoped to that form action — an inline Card with error state is more contextually appropriate than a toast.
- Toasts are for transient background events. A workspace creation failure is a foreground, blocking event — the user cannot proceed without resolving it.

***Inline error card specification:***

| ***Attribute**** | ****Value*** |
| --- | --- |
| Component | `Card` with `border-color: --fail (#e5484d)` |
| Background | `--fail-bg (rgba(229,72,77,0.10))` |
| Icon | Lucide `AlertTriangle` (16px, `--fail` color) |
| Message | `"We couldn't create your workspace. Please try again."` |
| CTA | Primary button — `"Retry"` (accent fill, `--accent #d9543f`) |
| Placement | Below the workspace slug input field, above the submit button |
| Behavior | Error card replaces the form's success state; retry re-submits the creation RPC |

***The submit button*** during the retry state uses a loading spinner (`data-variant="primary"` with `disabled` + spinner icon) — consistent with existing button patterns in the design system.

***QA assertion for outline 12 (workspace bootstrap failure):***

```
1. Trigger OAuth happy path but mock workspace creation to fail (via DB constraint or RPC error)
2. Assert: user is redirected to /onboarding (authenticated, no workspace)
3. Assert: the onboarding form is visible
4. Assert: after form submission fails — inline error card appears with "We couldn't create your workspace" message
5. Assert: "Retry" button is present and re-triggers the form
6. Assert: user is still authenticated (session cookie present)
```

---

### General design note on the OAuth buttons

Once [https://jira.upexgalaxy.com/browse/BK-3#icft=BK-3](https://jira.upexgalaxy.com/browse/BK-3#icft=BK-3) ships, the two OAuth buttons must:

- Remove `disabled={true`} and `cursor-not-allowed opacity-60`
- Remove `title="OAuth ships next sprint"`
- Have correct `onClick` handlers
- Update login copy from `"Magic link to your work email. OAuth and SSO ship next sprint."` to `"Sign in with your work email or a social provider."` (or similar — final copy to be confirmed by PO)

The button style stays as-is (`size="lg"`, full-width, default variant). No design changes needed to the button itself — just enable it.

---

**As Designer I confirm the component-level decisions above. QA has sufficient detail to write UI assertions for outlines 1–2, 5, 9–10, 12. No new design tokens or components are required for this story — all components (Button, Card, Sonner toast) are already in the design system.**

---

### Ely - 25/6/2026, 1:08:13

> ***NOTE:**** ****PO decision — 2026-06-24******:****** AC-7 reversed (automatic identity linking).***

Ely (Product Owner) decided to ***enable Supabase automatic identity linking*** (the platform default) instead of rejecting cross-provider same-email sign-ins.

- ***Before******:*** second provider with an already-registered verified email → rejected with `EMAIL_EXISTS` (manual linking by support).
- ***Now******:*** identities sharing the same VERIFIED email are auto-linked to one account across GitHub / Google / password. Same email = same user, seamless sign-in, no block.

***Rationale******:*** sign-in UX first — avoid dead-end errors and support tickets. Explicit/manual multi-provider management UI stays Phase 2.

Updated this issue: AC-7, Business Rules, Out Of Scope. Dev impact: the `email_exists` error path is removed from the OAuth flow (now unreachable). See ADR-0008.

---

### Automation for Jira - 25/6/2026, 1:16:29

🔎 Pull Request created. Task is pending to ANALYZE and REVIEW by the team. Waiting for PR Approval.

---

### Automation for Jira - 25/6/2026, 1:31:42

✅ Pull Request is successfully MERGED. Task is Done.

---

### Ely - 25/6/2026, 1:36:10

> ***INFO:**** ****Ready for QA on staging.*** @@Andrés Daniel Cumare Morales

***Merged******:**** PR #56 → `staging` (merge commit `d56316c`). ****Staging deploy******:*** verified READY, alias serving the new OAuth build.

***Test here******:*** https://staging-upexbunkai.vercel.app/login

***Config in place******:**** GitHub + Google OAuth providers enabled in Supabase; ****automatic identity linking is ON*** (AC-7 reversed by PO — same verified email across GitHub/Google/password = one account).

***Already validated (PO live E2E)******:*** AC-1 GitHub first-time → /onboarding, AC-3 returning → /projects, AC-2/AC-7 Google same-email → auto-linked to the same account. Server cases AC-4 (denied), AC-5 (403 state mismatch), AC-9 (init failure) verified via direct callback checks. AC-10 buttons/copy verified.

***Focus for QA******:*** AC-6 (third-party-cookie fallback within 30s), real consent-denial in-browser, and the magic-link/password rails still working alongside OAuth.

***Note******:*** includes a sign-out fix (sign-out now redirects to /login immediately). Implementation plan + ADR-0008 + compliance matrix are in the BK-3 PBI folder.

---

### Nahuel Gomez - 7/7/2026, 2:04:49

## Sprint 3 Wave 1 — Status Note

BK-3 is the remaining Wave 1 ticket not yet QA-tested. The rest of Wave 1 (BK-14, BK-22, BK-36, BK-57/58/59) have been processed.

Since BK-3 is assigned to Andrés, no action was taken to avoid overstepping. Ely has already validated the feature E2E (PR#56, identity linking ON) — the quickest path to close would be a QA pass from Andrés or re-assignment.

CC: @Andrés for awareness.


---

### Nahuel Gomez - 11/7/2026, 0:57:57

## QA Verdict: PASSED WITH NOTES

***Tested:**** 2026-07-10 | ****Environment:**** staging | ****Tester:*** Nahuel Gomez (autonomous=full)

### Results
- ***AC-1*** GitHub initiation: ✅ PASS
- ***AC-2*** Google initiation: ✅ PASS  
- ***AC-5*** CSRF state tokens: ✅ PASS
- ***AC-9*** OAuth initiation errors: ✅ PASS
- ***AC-10*** UI buttons visible/enabled: ✅ PASS
- ***AC-3/4/6/7/8***: ⚠️ Require real OAuth consent (provider interaction). Ely validated E2E per Sprint 3 comments.

### Summary
OAuth infrastructure works: both provider buttons redirect to OAuth consent pages, CSRF state tokens are unique per request, Supabase Auth callback configured. No blocking defects found.

***Evidence:*** screenshots in PBI evidence folder.
***Recommendation:*** QA Approved — request manual E2E on real accounts before prod release.

---

### Nahuel Gomez - 11/7/2026, 1:16:51

Login page with OAuth buttons — AC-10



---

### Nahuel Gomez - 11/7/2026, 1:40:45

## Acceptance Test Plan — BK-3: OAuth (GitHub/Google)

### ROI Analysis & Automation Verdicts

#### Candidate (7) — automatable via Playwright API/UI

| TC | Title | ROI | Why |
| --- | --- | --- | --- |
| TC-10 | CSRF state mismatch → 403 | 16 | Direct callback call, no browser needed |
| TC-11 | Missing state/code → error | 14 | Direct callback call |
| TC-12 | Replayed auth code rejected | 12 | Direct callback call, ST coverage |
| TC-13 | Expired state token rejected | 10 | Direct callback call |
| TC-27 | OAuth buttons enabled | 8 | Simple UI presence check |
| TC-28 | Login copy updated | 6 | Single string assertion |
| TC-31 | Open-redirect guard on `next` param | 9 | Direct callback call |

#### Manual (23) — needs real OAuth consent or human judgment

| TC | Title | Why Manual |
| --- | --- | --- |
| TC-1/2/3 | First-time OAuth E2E (GH/Google) | Needs real provider consent + redirect |
| TC-4/5/6 | Returning user flows | Needs real OAuth session |
| TC-7/8/9 | Consent denied paths | Needs real provider interaction |
| TC-14/15/16/17 | 3rd-party cookie fallback | Needs manual browser config |
| TC-18/19/20/21 | Cross-provider auto-link | Needs 2 real OAuth accounts |
| TC-22/23 | Bootstrap failure/retry | Needs OAuth + error trigger |
| TC-25/26 | Initiation failure | Needs provider 5xx |
| TC-29/30 | Magic-link/password regression | Manual regression check |

#### Deferred (2) — speculative or needs confirmation

| TC | Title | Why Deferred |
| --- | --- | --- |
| TC-24 | Server-side logging | Needs log viewer tool |
| TC-32 | Rate-limit on CSRF probing | Needs PO/dev confirmation if implemented |

***Recommendation******:*** 7 Candidate → feed test-automation. 23 Manual → manual regression suite. 2 Deferred → revisit when implementation confirmed.

---

### Nahuel Gomez - 11/7/2026, 1:57:45

## QA Verdict: PASSED WITH NOTES

***Tested******:**** 2026-07-10 | ****Environment******:**** staging | ****Tester******:*** Nahuel Gomez

### Results

***AC-1*** GitHub initiation: ✅ PASS
***AC-2*** Google initiation: ✅ PASS
***AC-5*** CSRF state tokens: ✅ PASS
***AC-9*** OAuth initiation errors: ✅ PASS
***AC-10*** UI buttons visible/enabled: ✅ PASS
***AC-3/4/6/7/8***: Require real OAuth consent. Ely validated E2E per Sprint 3 comments.

### Summary

OAuth infrastructure works: both provider buttons redirect to OAuth consent, CSRF state unique per request, Supabase Auth callback configured. No blocking defects.

***Recommendation******:*** QA Approved — request manual E2E on real accounts before prod release.

---


_Synced from Jira by sync-jira-issues_
